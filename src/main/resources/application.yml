spring:
  cache:
    type: redis
    redis:
      time-to-live: 60000
      cache-null-values: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/payment-manager?createDatabaseIfNotExist=true
